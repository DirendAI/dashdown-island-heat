# data: DuckDB + caches are reproducible via `heat-island add-city`
# (anchored to repo root — an unanchored `data/` would also swallow the
# static site's pre-rendered query snapshots under site/_dashdown/data/)
/data/

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage

# editors / os
.DS_Store
.idea/
.vscode/

# scratch
*.log
